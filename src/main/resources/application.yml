spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/atletiek_fribourg_data
    username: root
    password: +bomenzijnmooi+
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
